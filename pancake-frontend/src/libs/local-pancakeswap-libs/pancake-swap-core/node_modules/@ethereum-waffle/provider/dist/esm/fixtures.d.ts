import { Wallet } from 'ethers';
import { MockProvider } from './MockProvider';
declare type Fixture<T> = (provider: MockProvider, wallets: Wallet[]) => Promise<T>;
export declare const loadFixture: <T>(fixture: Fixture<T>) => Promise<T>;
export declare function createFixtureLoader(overrideProvider?: MockProvider, overrideWallets?: Wallet[]): <T>(fixture: Fixture<T>) => Promise<T>;
export {};
